Operator definition library.

('precedence (a$) < 'precedence (b$)) = ((x a$ y b$ z) = (x a$ (y b$ z))),
'right-associative (op$) = ((x op$ y op$ z) = (x op$ (y op$ z))),

(a <op< b) = ('precedence a < 'precedence b)
